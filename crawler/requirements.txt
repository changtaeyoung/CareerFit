# requirements.txt
# Java의 build.gradle dependencies {} 블록과 같은 역할
# pip install -r requirements.txt 로 한번에 설치
#
# 형식: 패키지명==버전
# == 는 정확한 버전 고정 (권장)
# >= 는 최소 버전 이상
# 
# 버전을 고정하는 이유: 
# - 팀원끼리 동일한 환경 보장
# - 배포 시 예상치 못한 업데이트로 인한 버그 방지

# 웹 프레임워크 (Spring Boot Web Starter와 동일)
fastapi==0.115.0

# ASGI 서버 (Spring Boot 내장 Tomcat과 동일 — 실제 HTTP 요청 처리)
uvicorn==0.32.0

# 환경변수 관리 (application.yml 대신 .env 파일 사용)
pydantic-settings==2.6.1

# HTTP 클라이언트 (RestTemplate / WebClient 역할)
httpx==0.27.2

# HTML 파싱 (크롤링용)
beautifulsoup4==4.12.3
lxml==5.3.0

# OpenAI SDK (AI 분석용 — 나중에 사용)
openai==1.54.3

# PostgreSQL 드라이버 (JDBC 역할)
psycopg2-binary==2.9.10

# 데이터 검증 (DTO validation 역할)
pydantic==2.9.2
